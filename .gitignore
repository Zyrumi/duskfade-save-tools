# Local runtime state -- machine-specific paths, save data, backups
config.json
capture_state.json
library_names.json
Library/
Backups/

# Python-based LiveSplit autosplitter (external Server-component approach) --
# superseded by the native Duskfade.asl port, not part of the public toolset
Duskfade.lss
duskfadelayout.lsl
splits.json
autosplitter.py
run_autosplitter.bat
gworld.py
watch_ending.py

# Packaged-release-specific readme (superseded by README.md for the source repo)
README.txt

# Local scratch space for features pulled out of the main tool but not deleted
Disabled Features/

# Packaged build output (ship via GitHub Releases, not source control)
Duskfade Save Loader/
_update_download.exe
*_update.exe
_apply_update.bat
build/
dist/
release/
*.spec

__pycache__/
*.pyc
